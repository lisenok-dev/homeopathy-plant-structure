# Инструкции по запуску

## 🚀 Быстрый старт

### Клонирование репозитория

```bash
git clone https://github.com/<username>/miasma-research-project.git
cd miasma-research-project
```

### Установка зависимостей

```bash
# Рекомендуется: pnpm (быстрее и эффективнее)
npm install -g pnpm
pnpm install

# Или стандартным способом
npm install

# Или yarn
yarn install
```

### Запуск в режиме разработки

```bash
# pnpm
pnpm dev

# npm
npm run dev

# yarn
yarn dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

### Сборка для продакшена

```bash
# pnpm
pnpm build

# npm
npm run build

# yarn
yarn build
```

Собранные файлы будут созданы в папке `dist/`

### Предварительный просмотр сборки

```bash
# pnpm
pnpm preview

# npm
npm run preview

# yarn
yarn preview
```

## 📋 Системные требования

- **Node.js** версии 18.0.0 или выше
- **npm**, **yarn** или **pnpm** для управления пакетами
- Современный веб-браузер (Chrome, Firefox, Safari, Edge)

## 🔧 Конфигурация

### Переменные окружения (необязательно)

Создайте файл `.env.local` в корне проекта для настройки:

```env
VITE_API_URL=http://localhost:3000
VITE_APP_TITLE=Miasma Research Project
```

### Настройка порта разработки

По умолчанию приложение запускается на порту 5173. Для изменения создайте файл `.env.local`:

```env
VITE_PORT=3000
```

## 📱 Команды для разработки

```bash
# Режим разработки с hot reload
pnpm dev

# Сборка для продакшена
pnpm build

# Предварительный просмотр сборки
pnpm preview

# Линтинг кода
pnpm lint

# Очистка кэша и зависимостей
pnpm clean
```

## 🌐 Развертывание

### GitHub Pages

1. Установите `gh-pages`:
```bash
npm install --save-dev gh-pages
```

2. Добавьте скрипт в `package.json`:
```json
{
  "scripts": {
    "deploy": "gh-pages -d dist"
  }
}
```

3. Разверните:
```bash
npm run build
npm run deploy
```

### Vercel

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Разверните:
```bash
vercel
```

### Netlify

1. Установите Netlify CLI:
```bash
npm install -g netlify-cli
```

2. Разверните:
```bash
netlify deploy --prod --dir=dist
```

## 🐛 Устранение неполадок

### Проблемы с зависимостями

**Ошибка**: `node_modules not found`
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
```

**Ошибка**: `pnpm lockfile not found`
```bash
pnpm install
```

### Проблемы с TypeScript

**Ошибка**: `Type 'any' is not assignable`
```bash
# Проверьте типы в файлах
pnpm tsc --noEmit
```

### Проблемы с Vite

**Ошибка**: `Module not found`
```bash
# Очистите кэш Vite
rm -rf node_modules/.vite
pnpm dev
```

## 📁 Структура файлов

```
miasma-research-project/
├── public/
│   ├── data/              # JSON данные
│   └── images/            # Изображения
├── src/
│   ├── components/        # React компоненты
│   ├── pages/            # Страницы
│   ├── App.tsx           # Главный компонент
│   └── main.tsx          # Точка входа
├── dist/                 # Собранные файлы (после build)
├── node_modules/         # Зависимости
├── package.json          # Конфигурация проекта
├── .gitignore           # Игнорируемые файлы
├── .env.local           # Переменные окружения (не в git)
├── README.md            # Документация
├── CONTRIBUTING.md      # Руководство по вкладу
├── CHANGELOG.md         # Журнал изменений
└── LICENSE              # Лицензия MIT
```

## 🔗 Полезные ссылки

- [Документация React](https://reactjs.org/docs/)
- [Документация Vite](https://vitejs.dev/guide/)
- [Документация TypeScript](https://www.typescriptlang.org/docs/)
- [Документация Tailwind CSS](https://tailwindcss.com/docs)
- [React Router](https://reactrouter.com/)

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте [Issues](https://github.com/<username>/miasma-research-project/issues)
2. Создайте новый Issue с подробным описанием
3. Приложите скриншоты и логи ошибок

---

**Совет**: Используйте `pnpm` для более быстрой установки и управления зависимостями!
